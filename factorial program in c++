#include <iostream>
using namespace std;
int solve(int n)
{
   
   return (n==0 ? 1 : n*solve(n-1));
}

int main() {
	// your code goes here
	int t;
	cin>>t;

	while(t--)
	{int p;
	cin>>p;
	    cout<<solve(p)<<'\n';
	}
	return 0;
}
